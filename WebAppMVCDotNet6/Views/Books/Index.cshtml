@model IEnumerable<BookProject.Models.Models.Book>
@{
    ViewData["Title"] = "Index";
}
<partial name="_AlertPartial" />
<div class="container p-3">
    <div class="row pt-4">


        <div class="col-6">
            <h2 class="text-primary">Book List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Books" asp-action="InsertUpt" class="btn btn-primary"><i class="bi bi-plus"></i>Create/Edit</a>
        </div>

    </div>
</div>


<table id="tblData" class="table table-bordered table-striped" style="width:100%">
    <thead>
    <tr>
        <th>
             BookTitle
        </th>
        <th>
            Description
        </th>
        <th>
            Price
        </th>
        <th>
            Category
        </th>
       @* <th>
            Edit
        </th>
       <th>
           Delete
       </th>*@



    </tr>
    </thead>
    @*<tbody>
        @foreach (var c in Model)
        {
                    <tr>
                        <td><a asp-controller="Categories" asp-action="Details" asp-route-id="@c.Id">@c.BookTitle</a></td>
                        <td>@c.Description</td>
                        <td>@c.Price</td>
                        <td>@c.Category</td>

                            <div class="btn-group m-75" role="group">
                                    <td><a asp-controller="Books" asp-action="Edit" asp-route-id="@c.Id" class=" btn btn-primary"><i class="bi bi-pencil"></i>Edit</a></td>
                                    <td> <a asp-controller="Books" asp-action="Delete" asp-route-id="@c.Id" class=" btn btn-primary"><i class="bi bi-trash3"></i>Delete</a></td>
                            </div>

                     </tr>

        }
    </tbody>*@

</table>
@section Scripts{
    <script src="~/js/book.js"></script>
}